# Planning/reference material — kept locally, not part of the public repo.
# sci/ in particular holds copyrighted paper PDFs that shouldn't be redistributed.
/sci/
/roadmap/
/design/

# Another AI's separate implementation attempt — not part of this repo's tracked history.
/deepseek/

# Rust
/target/
**/target/
Cargo.lock

# Locally-installed dev tooling (e.g. cbindgen, installed via `cargo install --root` to keep
# it off the boot volume in this environment) — machine-specific, not part of the repo.
/.tooling/

# Python
__pycache__/
*.pyc
.venv/
venv/
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.hypothesis/

# Compiled pyo3/maturin extension artifacts (built via `maturin develop`, not source)
*.so
*.pyd
*.dylib

# Editors / OS
.DS_Store
.vscode/
.idea/
.claude/
