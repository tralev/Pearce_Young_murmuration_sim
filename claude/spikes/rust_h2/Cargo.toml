[workspace]

[package]
name = "rust_h2_spike"
version = "0.1.0"
edition = "2021"
publish = false

# Deliberately NOT a workspace member (../../Cargo.toml) — same "throwaway spike" status as
# ../numpy_prototype/pearce_oracle.py: not linked into murmur_young, not run by CI, own
# Cargo.lock. Purpose (roadmap.md §3 item 5 / §6.2 Phase 14): survey a Rust sparse-eigensolver
# candidate, prove it reproduces known closed-form Laplacian spectra and matches h2.py's
# output on identical input, before committing to wiring one into `murmur_core`/`murmur_young`.

[dependencies]
nalgebra = "0.35"

[dev-dependencies]
