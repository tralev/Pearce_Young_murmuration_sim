[package]
name = "murmur_ceiling_speed"
edition.workspace = true
version.workspace = true
license.workspace = true
description = "CeilingSpeed: cap-only SpeedModel plugin (roadmap.md Phase 16, design/02_plugins.md pymurmur plugins/speed_model.py) — clamps overspeed boids down, never boosts underspeed ones, unlike BandSpeed's range clamp."

[dependencies]
murmur_core = { path = "../../murmur_core" }

[dev-dependencies]
murmur_conformance = { path = "../../murmur_conformance" }
