[package]
name = "murmur_radius_gather"
edition.workspace = true
version.workspace = true
license.workspace = true
description = "RadiusGather: the first NeighborSelection plugin (design/01_core.md §6.1). Queries a SpatialIndex, applies the exact distance test, and fills bearing/velocity explicitly."

[dependencies]
murmur_core = { path = "../../murmur_core" }

[dev-dependencies]
murmur_conformance = { path = "../../murmur_conformance" }
murmur_hash_grid = { path = "../murmur_hash_grid" }
