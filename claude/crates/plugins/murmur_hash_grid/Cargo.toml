[package]
name = "murmur_hash_grid"
edition.workspace = true
version.workspace = true
license.workspace = true
description = "HashGrid: the first SpatialIndex plugin (design/01_core.md §5). Unbounded hash-mapped cell grid, no scientific specificity of its own — built first for simplicity, not privilege."

[dependencies]
murmur_core = { path = "../../murmur_core" }

[dev-dependencies]
murmur_conformance = { path = "../../murmur_conformance" }
