[package]
name = "murmur_spatial"
edition.workspace = true
version.workspace = true
license.workspace = true
description = "Spatial: classic topological Reynolds FlockingMode (separation/alignment/cohesion) via murmur_core's kernel toolkit (roadmap.md Phase 17)."

[dependencies]
murmur_core = { path = "../../murmur_core" }
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
murmur_conformance = { path = "../../murmur_conformance" }
murmur_pearce = { path = "../murmur_pearce" }
murmur_vicsek = { path = "../murmur_vicsek" }
