[package]
name = "murmur_none_speed"
edition.workspace = true
version.workspace = true
license.workspace = true
description = "NoneSpeed: no-enforcement SpeedModel plugin (roadmap.md Phase 16, design/02_plugins.md pymurmur plugins/speed_model.py) — a true no-op, proving the seam is optional, not load-bearing, for a caller that wants raw unclamped integration."

[dependencies]
murmur_core = { path = "../../murmur_core" }

[dev-dependencies]
murmur_conformance = { path = "../../murmur_conformance" }
murmur_ceiling_speed = { path = "../murmur_ceiling_speed" }
murmur_fixed_speed = { path = "../murmur_fixed_speed" }
