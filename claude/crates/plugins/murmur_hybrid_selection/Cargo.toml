[package]
name = "murmur_hybrid_selection"
edition.workspace = true
version.workspace = true
license.workspace = true
description = "HybridSelection: metric + topological NeighborSelection plugin with an optional forward-facing FOV cone (roadmap.md Phase 16, design/02_plugins.md pymurmur plugins/neighbor_selection.py's richest selector)."

[dependencies]
murmur_core = { path = "../../murmur_core" }

[dev-dependencies]
murmur_conformance = { path = "../../murmur_conformance" }
murmur_hash_grid = { path = "../murmur_hash_grid" }
murmur_kdtree_index = { path = "../murmur_kdtree_index" }
murmur_radius_gather = { path = "../murmur_radius_gather" }
murmur_knn_selection = { path = "../murmur_knn_selection" }
