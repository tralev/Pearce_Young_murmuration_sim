[package]
name = "murmur_sphere_domain"
edition.workspace = true
version.workspace = true
license.workspace = true
description = "SphereBoundary: hard-projection-to-surface Domain plugin (roadmap.md Phase 16, design/02_plugins.md pymurmur plugins/boundary/strategies.py::SphereBoundary)."

[dependencies]
murmur_core = { path = "../../murmur_core" }

[dev-dependencies]
murmur_conformance = { path = "../../murmur_conformance" }
