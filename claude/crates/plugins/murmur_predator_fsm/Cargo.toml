[package]
name = "murmur_predator_fsm"
edition.workspace = true
version.workspace = true
license.workspace = true
description = "RicherPredator: pymurmur-superset predator/prey StepHook (roadmap.md Phase 15) — approach/egress predator FSM + push/wake/split/wave prey force bundle + blackening, grounded in Papadopoulou et al. 2024's starling collective-escape mechanisms (pymurmur's own physics/extensions/predator.py source is unavailable in this environment — see module doc). Second StepHook occupant alongside the Phase 8 minimal `predator`, which stays as the validated baseline."

[dependencies]
murmur_core = { path = "../../murmur_core" }

[dev-dependencies]
murmur_conformance = { path = "../../murmur_conformance" }
murmur_pearce = { path = "../murmur_pearce" }
murmur_instant_response = { path = "../murmur_instant_response" }
murmur_open_domain = { path = "../murmur_open_domain" }
murmur_hash_grid = { path = "../murmur_hash_grid" }
murmur_radius_gather = { path = "../murmur_radius_gather" }
murmur_initializers = { path = "../murmur_initializers" }
