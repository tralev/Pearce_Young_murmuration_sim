[package]
name = "murmur_margin_domain"
edition.workspace = true
version.workspace = true
license.workspace = true
description = "MarginBoundary: soft-nudge + hard-clamp cube Domain plugin (roadmap.md Phase 16, design/02_plugins.md pymurmur plugins/boundary/strategies.py::MarginBoundary)."

[dependencies]
murmur_core = { path = "../../murmur_core" }

[dev-dependencies]
murmur_conformance = { path = "../../murmur_conformance" }
