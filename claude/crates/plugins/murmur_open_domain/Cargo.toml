[package]
name = "murmur_open_domain"
edition.workspace = true
version.workspace = true
license.workspace = true
description = "OpenSpace: the first Domain plugin (design/01_core.md §3). Unbounded R^3, selected as the slice default because the science requires open, self-sizing space."

[dependencies]
murmur_core = { path = "../../murmur_core" }

[dev-dependencies]
murmur_conformance = { path = "../../murmur_conformance" }
