[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "murmuration"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = ["numpy>=1.24"]

[tool.maturin]
python-source = "../../python"
module-name = "murmuration._core"
manifest-path = "Cargo.toml"
